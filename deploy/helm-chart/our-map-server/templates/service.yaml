apiVersion: v1
kind: Service
metadata:
  name: {{ include "our-map-server.fullname" . }}
  labels:
    {{- include "our-map-server.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "our-map-server.selectorLabels" . | nindent 4 }}
